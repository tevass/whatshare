'use client'

import * as Primitive from '@radix-ui/react-popover'
import { ComponentProps } from 'react'

export type PopoverProps = ComponentProps<typeof Primitive.Root>

export const Popover = Primitive.Root
